# Agent instructions

## Agent skills

### Triage labels

Five canonical roles: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Architectural decision records live in `docs/adr/` at the repo root. See `docs/agents/domain.md`.
